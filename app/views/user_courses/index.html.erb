<h3>Crawl dữ liệu thành công.</h3>
 <h3>Bạn đã đăng kí tổng cộng <%= current_user.user_courses.count %> môn học</h3>
 <table class="table table-hover table-bordered">
  <thead>
    <tr>
      <th></th>
      <% 1.upto(10) do |index| %>
        <th><%= index %></th>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <% 2.upto(7) do |dow| %>
      <tr>
        <td>Thứ <%= dow %></td>
        <% 1.upto(10) do |time| %>
          <td colspan="1" id="T<%= dow %>-t<%= time %>">
          </td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
<script><%= render "class_arrange.js" %></script>
