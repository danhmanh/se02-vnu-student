<nav class="navbar navbar-default">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to "VNU", root_path, class: "navbar-brand" %>
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <% if logged_in? %>
            <li><%= link_to "New Post", new_post_path %></li>
            <li><%= link_to "Get timetable", timetable_path %></li>
            <li><%= link_to "Chat room", chatroom_path %></li>
          <% end %>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <% if logged_in? %>
              <p class="navbar-text">
                Hi, <%= current_user.name %> - <%= current_user.str_id %>
              </p>
            <%= link_to "Log Out", logout_path, class: "btn btn-danger navbar-btn",
              method: :delete %>
          <% else %>
            <%= link_to "Log In", login_path, class: "btn btn-default navbar-btn" %>
          <% end %>
        </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav>
